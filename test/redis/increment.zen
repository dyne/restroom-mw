Rule caller restroom-mw
Given I have a valid redis connection on 'redis://localhost:6379'
Given I have a 'number' named 'incremented'
Given I read into 'incremented' and increment the key named by 'key'
Given I have a 'number' named 'succ'
Given I read into 'succ' and increment the key named by 'key'
Then print data


