Rule unknown ignore 
Given I have a ethereum endpoint named 'fabchain'
Given I have a 'string' named 'poem'
Given I read from 'ethereum' the data in tag 'myTag' and save the output into 'poem'
Then print data
