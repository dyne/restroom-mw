Rule caller restroom-mw

Given I connect to influxdb with the connection object named 'influx'

Given I run the array of query named 'query_array' and save the output into 'result'
Given I have a 'string dictionary' named 'result'

Then print the 'result'
