Rule unknown ignore 
Given I have a 'string' named 'my decimals'
Given I have a 'string' named 'my name'
Given I have a 'string' named 'my symbol'
Given I have a 'string' named 'my total supply'
Given I have a 'string' named 'decimals'
Given I have a 'string' named 'name'
Given I have a 'string' named 'symbol'
Given I have a 'string' named 'total supply'
Given I have a 'string' named 'my balance'
Given I have a 'string' named 'balance'
When I have a ethereum endpoint named 'fabchain'
When I have the 'decimals' for erc20 'contract_address' named 'my_decimals'
When I have the 'name' for erc20 'contract_address' named 'my_name'
When I have the 'symbol' for erc20 '0x8AadD73AB45FE7Bd2048267026BfC1a8fF244BB6' named 'my_symbol'
When I have the 'total supply' for erc20 'contract_address' named 'my_total_supply'
When I have the 'balance' of 'owner' for erc20 'contract_address' named 'my_balance'
When I have the 'decimals' for erc20 'contract_address'
When I have the 'name' for erc20 'contract_address'
When I have the 'symbol' for erc20 'contract_address'
When I have the 'total supply' for erc20 '0x8AadD73AB45FE7Bd2048267026BfC1a8fF244BB6'
When I have the 'balance' of 'someone' for erc20 'contract_address'
Then print data
