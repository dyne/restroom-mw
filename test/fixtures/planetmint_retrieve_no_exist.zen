Rule unknown ignore

# Restroom onBefore
Given I have a planetmint endpoint named 'endpoint'
Given I read the planetmint transaction 'txId' and save the output into 'out'

# Zenroom
Given I have a 'string dictionary' named 'out'
When I create the hash of 'out'
Then print the data