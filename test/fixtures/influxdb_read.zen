Scenario 'InfluxDB': Connect to influx and read data
Rule unknown ignore

Given I have a 'string dictionary' named 'influx'
Given I have a 'string' named 'query'
Given I have a 'string dictionary' named 'result'
Given I have a 'string' named 'query2'
Given I have a 'string dictionary' named 'result2'
Given I connect to influxdb with the connection object named 'influx'

When I run the query named 'query' and save the output into 'result'
When I run the query named 'query2' and save the output into 'result2'
When I create the random object of '256' bits

Then print all the 'result2'
