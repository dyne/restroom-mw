Rule unknown ignore

# Restroom onBefore
Given I have a planetmint endpoint named 'endpoint'
Given I create the ed25519 keypair

# Zenroom
Given I have a 'base58 dictionary' named 'ed25519 keypair'
Given I have a 'base58 dictionary' named 'sender'
Given I have a 'string' named 'endpoint'
Given I have a 'string' named 'txid'
Given I have a 'string' named 'amount'
Given I have a 'base58' named 'public key' in 'ed25519_keypair'

When I rename 'ed25519_keypair' to 'receiver'
When I rename 'sender' to 'ed25519_keypair'


# Restrom onSuccess
Then I create the planetmint transaction to transfer the amount 'amount' of 'txid' to 'public_key'
Then I create the signed planetmint transaction of 'planetmint_transaction'
Then ask planetmint to broadcast the 'signed_planetmint_transaction'
# Zenroom
Then print the data


