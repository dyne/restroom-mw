Rule unknown ignore
Given that I have a sawroom endpoint named 'myEndpoint'
When I create the random object of '128' bits
Then I ask sawroom to deposit '3' coins to the wallet 'myKey' and store the result in 'myOutput'
Then print all data

