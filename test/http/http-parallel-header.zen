Rule unknown ignore
Given I have a 'string dictionary' named 'timestamps'
Given I have a 'string dictionary' named 'header'
Given I execute parallel GET to 'get_url' with header 'header' and save the result named 'one' within the object 'timestamps'
Given I execute parallel GET to 'get_url' with header 'header' and save the result named 'two' within the object 'timestamps'
Then print the 'timestamps'
